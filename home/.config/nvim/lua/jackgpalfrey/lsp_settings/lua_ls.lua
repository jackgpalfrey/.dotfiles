-- https://luals.github.io/wiki/settings/
return {
    settings = {
        Lua = {
            runtime = { version = "LuaJIT", },
            hint = {
                enable = true,
                semicolon = "All",
                setType = true,
            },
        }
    }
}
